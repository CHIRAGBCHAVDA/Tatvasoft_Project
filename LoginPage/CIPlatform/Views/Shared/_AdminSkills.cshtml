@using CIPlatform.Models.AdminViewModels
@model PageList<AdminSkillsViewModel>

<div id="v-pills-missionskills" >
    <div class="missionskills-header border-bottom border-light">
        <span class="h2 border-bottom border-dark">Skills</span>
    </div>
    <div>

    </div>
    <div class="d-flex search-missionskills-and-add mt-3 justify-content-between">
        <div class="search-missionskills d-flex align-items-center border ps-3">
            <img src="/assets/search.png" alt="" style="height:14px">&nbsp;&nbsp;
            <input class="form-control border border-0" type="search" placeholder="Search" aria-label="Search" id="search-missionskills-query">
        </div>
        <div class="missionskills-add">
            <button class="btn bg-white text-orange border-orange px-3 rounded-pill" id="admin-missionskills-add" data-bs-toggle="modal" data-bs-target="#missionSkillsAddEditModal-0"> <i class="bi bi-plus"></i> Add </button>
        </div>
    </div>
    <div class="missionskills-table mt-4" id="admin-missionskills-table-body-wrapper">
        @*<table class="table" style="">
            <thead>
                <tr>
                    <th scope="col">Skill Name</th>
                    <th scope="col">Status</th>
                    <th scope="col">Action</th>
                </tr>
            </thead>
            <tbody >*@
                <partial name="_AdminMissionSkillTablePartial" model="Model" />
            @*</tbody>
        </table>

         @{
            double d = Model.Count/ 10.0;
            double pages = Math.Ceiling(d);
        }


        <div class="pagination-nav d-flex flex-row-reverse w-100">
            <nav aria-label="Page navigation example">
                <ul class="pagination">
                    <li class="page-item-missionskill d-flex">
                        <a class="page-link" href="#" aria-label="Previous">
                            <span aria-hidden="true"><<</span>
                        </a>
                    </li>
                    <li class="page-item-missionskill d-flex">
                        <a class="page-link" href="#" aria-label="Previous">
                            <span aria-hidden="true"><</span>
                        </a>
                    </li>
                    @for (int i = 1; i <= @pages; i++)
                    {
                        <li class="page-item-missionskill" id="@i"><a class="page-link">@i</a></li>
                    }

                    <li class="page-item-missionskill d-flex">
                        <a class="page-link" href="#" aria-label="Next">
                            <span aria-hidden="true">></span>
                        </a>
                    </li>
                    <li class="page-item-missionskill d-flex">
                        <a class="page-link" href="#" aria-label="Next">
                            <span aria-hidden="true">>></span>
                        </a>
                    </li>
                </ul>
            </nav>

        </div>*@
    </div>
</div>


<partial name="_AdminMissionSkillAddEditForm" model="new AdminSkillsViewModel(){}" />